package cmd

// compose.go — intentionally empty.
//
// Multi-container challenge builds are now handled server-side by the Nexus Engine.
// This allows the CLI to remain a zero-privilege client, while the Engine
// (running as root) handles container image orchestration securely.
